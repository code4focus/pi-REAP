# PR 2 Scope Packet: Provider Patch Layer

scope_packet_version: 1
source_plan: docs/plan/pi-REAP-v1.0.md
source_plan_sha256: 184c964814cd1752b89409fec352cafb11f8b1cffe91b55abb660b34dfb290f6
plan_pr: 2

## Context contract

Load this packet, the assigned role skill, and only provider/test files needed for the task. Do not preload the full plan, dispatch policy, blocker ledger, or another PR packet. Open an on-demand plan range only for missing exact semantics.

## Deliverables

- Supported API/model guards.
- Effort mapping and payload-shape guards.
- Request-local `reasoning.effort` patching.
- Payload fingerprinting and preservation properties.
- Sanitized real fixture snapshots for both supported APIs when available.

## Required boundaries

- Support only `openai-codex-responses` and `openai-responses`.
- Change only `reasoning.effort`; preserve every other payload field and cache surface.
- Leave unknown, unsupported, invalid, or conflicted payloads unchanged.
- Do not call `setThinkingLevel()`.
- Do not implement classifier behavior; inject target effort directly in tests.
- Label synthetic fixtures as synthetic and never present them as real captures.

## Acceptance evidence

- Both supported APIs are recognized.
- Unknown payloads remain unchanged.
- Structural comparison and canonical hash prove only `reasoning.effort` differs.
- Preservation property tests pass.
- Fixture provenance and sanitization are explicit.

## Expected ownership

- `src/provider/`.
- Provider unit/property tests.
- `test/fixtures/openai-responses/`.
- `test/fixtures/openai-codex-responses/`.

## On-demand plan ranges

- Request-local and preservation invariants: lines 70-137.
- Provider adaptation and patch test: lines 766-862.
- Provider, preservation, and baseline tests: lines 1237-1281.
- Final payload fixtures: lines 1309-1329.
- PR 2 contract: lines 1493-1514.
- Provider conflict and shape risks: lines 1611-1630.

