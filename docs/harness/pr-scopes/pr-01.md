# PR 1 Scope Packet: Repository Skeleton and Contract Tests

scope_packet_version: 1
source_plan: docs/plan/pi-REAP-v1.0.md
source_plan_sha256: 184c964814cd1752b89409fec352cafb11f8b1cffe91b55abb660b34dfb290f6
plan_pr: 1

## Context contract

Load this packet, the assigned role skill, and only the source/test files needed for the current task. Do not preload the full plan, dispatch policy, blocker ledger, or another PR packet. Open an on-demand plan range only when exact semantics are missing below.

## Deliverables

- Package skeleton using TypeScript, TypeBox, and Vitest.
- Domain types and module boundaries from the frozen layout.
- Typed extension lifecycle harness.
- Read-only configuration loader.
- No-side-effect contract tests.
- Repository-wide build, lint, typecheck, and test commands.

## Required boundaries

- Do not implement provider payload patching or effort mapping.
- Do not implement classifier, epoch policy, telemetry, evaluation, or enforcement.
- Do not add `proper-lockfile` as a runtime dependency.
- Do not register an LLM-callable tool.
- Do not read or write Pi `settings.json`.

## Acceptance evidence

- `build`, `lint`, `typecheck`, and `test` all pass.
- Contract evidence proves no tool registration.
- Contract evidence proves no `settings.json` read or write.
- The typed harness represents handler order and chaining without a permissive `any`-only mock.

## Expected ownership

- Package and toolchain files.
- `src/config/` and foundational `src/domain/` types.
- `test/integration/extension-harness.ts`.
- Contract-focused unit/integration tests.

## On-demand plan ranges

- Module layout and core types: lines 250-444.
- Configuration contract: lines 863-912.
- Integration and negative tests: lines 1282-1308.
- Mandatory gates: lines 1435-1449.
- PR 1 contract: lines 1470-1492.

