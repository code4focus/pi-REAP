# PR 3 Scope Packet: Epoch Runtime and Deterministic Policy

scope_packet_version: 1
source_plan: docs/plan/pi-REAP-v1.0.md
source_plan_sha256: 184c964814cd1752b89409fec352cafb11f8b1cffe91b55abb660b34dfb290f6
plan_pr: 3

## Context contract

Load this packet, the assigned role skill, and only the runtime/policy/test files needed for the task. Do not preload the full plan, dispatch policy, blocker ledger, or another PR packet. Open an on-demand plan range only for missing exact semantics.

## Deliverables

- Task-epoch state machine and continuation detection.
- Deterministic feature extraction, hard floors, and classifier.
- Resume guard and epoch-local monotonic escalation.
- `low`, `medium`, `high`, and `xhigh` automatic routing.
- Session-only status and override commands.

## Required boundaries

- Make the initial decision before the first provider request.
- Keep routing state and status out of prompt, messages, tools, and history.
- Never lower effort within an epoch.
- Permit an independent task after settlement to start a lower epoch.
- Never select `max` automatically.
- Treat ambiguous resume as `xhigh` and uncertainty as conservative.
- Do not add telemetry persistence, evaluation machinery, or enforce rollout.

## Acceptance evidence

- Complex settled work followed by a simple independent task can become `low`.
- Continuation inherits the prior floor.
- Tool and provider failures escalate monotonically.
- Resume ambiguity becomes `xhigh`.
- Properties cover monotonicity, reset, and baseline-safe failure.
- `max` appears only through an explicit session command.

## Expected ownership

- `src/domain/` epoch/runtime types.
- `src/policy/`.
- `src/runtime/` lifecycle, commands, and status.
- Classifier, continuity, escalation, lifecycle, resume, and property tests.

## On-demand plan ranges

- Terminology and effective effort: lines 146-194.
- Core types: lines 328-444.
- Lifecycle and epoch boundaries: lines 443-675.
- Deterministic classification: lines 680-765.
- Session commands: lines 913-973.
- Unit/property tests: lines 1203-1281.
- PR 3 contract: lines 1515-1536.

