# PR 6 Scope Packet: Conservative Enforcement

scope_packet_version: 1
source_plan: docs/plan/pi-REAP-v1.0.md
source_plan_sha256: 184c964814cd1752b89409fec352cafb11f8b1cffe91b55abb660b34dfb290f6
plan_pr: 6

## Context contract

Load this packet, the assigned role skill, and only enforce/runtime/evaluation evidence needed for the task. Do not preload the full plan, dispatch policy, blocker ledger, or another PR packet. Open an on-demand plan range only for missing exact semantics.

## Deliverables

- Conservative enforce mode using the frozen initial routing table.
- Evidence-backed rollout from shadow recommendations to request-local patches.
- Regression and cache-crossover evidence.
- Reviewed under-routing cases captured as fixed regression fixtures.

## Frozen routing table

```text
high-confidence simple_query -> low
high-confidence bounded_read -> medium
implementation/debugging -> high
high-risk/failure -> xhigh
other -> high
```

## Required boundaries

- Satisfy every mandatory 1.0 gate before acceptance.
- Never route a high-risk corpus task automatically to `low` or `medium`.
- Preserve request-local patching, baseline fallback, monotonic epochs, explicit-only `max`, and telemetry privacy.
- Do not enable complex soft rules.
- Do not claim savings without cache read/write and request-count evidence.
- Do not absorb PR 7 compatibility, release, documentation, or rollback work.

## Acceptance evidence

- All mandatory gates pass.
- Cache crossover experiment is complete.
- Regression corpus passes within the frozen quality allowance.
- Every under-routing case has human review and a fixed regression fixture.
- Provider request count and cache behavior show no hidden amplification.

## Expected ownership

- Enforce-mode runtime/config integration.
- Enforce-mode integration tests.
- Regression fixtures and evidence reports required by the gate.

## On-demand plan ranges

- Frozen invariants: lines 50-145.
- Enforce semantics: lines 1001-1012.
- Cache validation: lines 1133-1200.
- Evaluation and 1.0 gates: lines 1330-1469.
- PR 6 contract: lines 1575-1595.
- Known rollout risks: lines 1609-1668.

